import { SimplePage } from "@/components/sections/simple-page";
export default function Page(){return <SimplePage title="Account register" subtitle="Authentication placeholder UI for future backend integration." points={["UI shell only (no auth provider connected).","Designed for future token/session strategy.","Localization-ready English-first copy."]}/>}
